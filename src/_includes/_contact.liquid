  <div class="container">
    <div class="box">
      <div class="column has-text-left">
        <p class="has-text-weight-bold">Kontaktinformationen</p>
        <p>
          {{ contact.street }},
          {{ contact.plz }} {{ contact.city }}</br>
          {{ contact.phone }}</br>
          {{ contact.email }}
        </p>
      </div>
    </div>
  </div>
